# QubicKit CLI (Placeholder)

This workspace will become the Bun-based command-line interface for automating QubicKit workflows—wallet derivation, proposal tooling, automation pipelines, and more. For now it simply depends on `@qubiq/core` so experimental work can start without affecting the main package.
